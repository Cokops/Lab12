ов"# Отчёт по Code Review

Ниже представлены найденные проблемы в сгенерированном коде и способы их исправления.

## 1. Неправильный URL для OAuth2 в схеме токена

**Что сгенерировал ИИ:**
```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="api/v1/auth/login")
```

**В чём проблема:**
Относительный путь `api/v1/auth/login` без начального слеша может привести к неправильной работе маршрутизации в FastAPI, особенно при использовании подключаемых роутеров. Корректный путь должен начинаться с `/`.

**Как исправил:**
Изменил на абсолютный путь:
```python
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")
```

## 2. Жёстко прописанный SECRET_KEY в коде

**Что сгенерировал ИИ:**
```python
SECRET_KEY: str = "09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7"
```

**В чём проблема:**
Хранение секретного ключа JWT прямо в коде представляет серьёзную уязвимость безопасности. При попадании кода в публичный репозиторий это позволяет злоумышленнику подписывать и расшифровывать токены.

**Как исправил:**
Заменил значение на заглушку и добавил переменную в файл `.env`, который должен игнорироваться git'ом. В продакшене ключ должен задаваться через переменные окружения.

## 3. Уязвимость при работе с базой данных (SQLite в продакшене)

**Что сгенерировал ИИ:**
```python
DATABASE_URL: str = "sqlite+aiosqlite:///./test_hotel_booking.db"
```

**В чём проблема:**
Использование SQLite в асинхронном приложении не рекомендуется для продакшена. Кроме того, использование тестовой БД в продакшене может привести к потере данных. Также в комментарии указано, что не установился `asyncpg`, что указывает на проблему с зависимостями.

**Как исправил:**
Добавил в `.env` переменную `DATABASE_URL`, чтобы можно было легко переключаться между средами. Рекомендовано использовать PostgreSQL с `asyncpg` в продакшене.

## 4. Синхронный вызов на асинхронном движке

**Что сгенерировал ИИ:**
```python
Base.metadata.create_all(bind=engine)
```

**В чём проблема:**
Метод `create_all` является синхронным и вызывается на `AsyncEngine`, что вызовет `AttributeError` при запуске приложения. Это критическая ошибка, делающая приложение незапускаемым.

**Как исправил:**
Удалил строку `Base.metadata.create_all(bind=engine)` из `main.py`. Создание таблиц должно выполняться асинхронно (через `run_sync`) или с использованием Alembic.

## 5. Отсутствующий импорт `model_validator`

**Что сгенерировал ИИ:**
```python
@model_validator(mode='after')
```

**В чём проблема:**
`model_validator` не импортирован из `pydantic`. Это вызовет `NameError` при импорте модуля `booking.py`.

**Как исправил:**
Добавил `model_validator` в импорт из `pydantic` в файле `schemas/booking.py`. Дублирование проверки существующего пользователя (пункт 3 первоначального отчёта) и неконсистентная обработка логики в `register_user` (пункт 5) описывают одну и ту же проблему. Решение — удаление строки `return None` перед `raise HTTPException`, чтобы оставался только один стиль обработки ошибок.

## 6. Синхронный вызов на асинхронном движке

**Что сгенерировал ИИ:**
```python
Base.metadata.create_all(bind=engine)
```

**В чём проблема:**
Метод `create_all` является синхронным и вызывается на `AsyncEngine`, что вызовет `AttributeError` при запуске приложения. Это критическая ошибка, делающая приложение незапускаемым.

**Как исправил:**
Удалил строку `Base.metadata.create_all(bind=engine)` из `main.py`. Создание таблиц должно выполняться асинхронно (через `run_sync`) или с использованием Alembic.


## 7. Отсутствующий импорт `model_validator`

**Что сгенерировал ИИ:**
```python
@model_validator(mode='after')
```

**В чём проблема:**
`model_validator` не импортирован из `pydantic`. Это вызовет `NameError` при импорте модуля `booking.py`.

**Как исправил:**
Добавил `model_validator` в импорт из `pydantic` в файле `schemas/booking.py`.
