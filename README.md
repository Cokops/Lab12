# Prompt Log — Проект 12Lab: Система бронирования отелей на FastAPI

**Описание проекта:** Создание полнофункционального веб-приложения для бронирования отелей с асинхронным бэкендом на FastAPI, SQLAlchemy 2.0 и PostgreSQL. Система включает управление номерами, тарифами, бронью, оплатой и отзывами.

**Студент:** Герасев Артем Александрович 
**Группа:** 221131р 
**Вариант:** 5 — Система бронирования отелей  
**Дата:** 14.05.2026

---

## Этап 1: Анализ и инициализация

### Промпт 1
**Инструмент:** GigaCode AI Agent
**Промпт:** "Проверь структуру папок и файлов в проекте 12Lab. Всё ли готово для создания веб-приложения на FastAPI?"
**Результат:** Анализ подтвердил наличие всех папок (`app/api/v1`, `models`, `schemas`, `services`, `core`, `utils`, `tests`, `alembic`) и файлов (`main.py`, `config.py`, `database.py`, `requirements.txt`). Структура корректна, зависимости в `requirements.txt` актуальны. Можно продолжать.

### Промпт 2
**Инструмент:** GigaCode AI Agent
**Промпт:** "Инициализируй основные компоненты приложения: создай `main.py` с точкой входа FastAPI, `config.py` с настройками через Pydantic Settings, и `database.py` с асинхронным подключением к PostgreSQL."
**Результат:** Создана точка входа FastAPI с корневым маршрутом `/`. Реализован `config.py` с JWT-настройками и параметрами БД. Настроен `database.py` с асинхронным движком SQLAlchemy 2.0, базовым классом моделей и зависимостью `get_db`.

**Итого по этапу 1:**
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Этап 2: Модели и схемы

### Промпт 3
**Инструмент:** GigaCode AI Agent
**Промпт:** "Создай SQLAlchemy-модели для проекта бронирования отелей: User (с ролями), Hotel, Room, Booking, Review. Используй enum для ролей и статусов. Настрой связи один-ко-многим и каскадное удаление."
**Результат:** Реализованы 5 моделей с перечислениями `UserRole` и `BookingStatus`. Настроены внешние ключи и отношения. Модели собраны в `__init__.py`.

### Промпт 4
**Инструмент:** GigaCode AI Agent
**Промпт:** "Создай Pydantic-схемы валидации для User, Hotel, Room, Booking, Review. Добавь схемы для создания, обновления и ответа. Настрой валидаторы email, рейтинга и обязательных полей."
**Результат:** Реализованы схемы с наследованием. Добавлены валидаторы: минимальная длина имени, проверка email-формата, ограничение рейтинга 1-5, положительная цена. Настроен `Config` с `orm_mode`.

**Итого по этапу 2:**
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Этап 3: Аутентификация и безопасность

### Промпт 5
**Инструмент:** GigaCode AI Agent
**Промпт:** "Реализуй безопасность: хеширование паролей через bcrypt, создание JWT-токенов, зависимости `get_current_user`, `get_current_admin`. Добавь схемы Token и TokenData."
**Результат:** Создан `security.py` с функциями хеширования, `verify_password`, `create_access_token`. Реализованы зависимости через `OAuth2PasswordBearer`. Схемы `Token` и `TokenData` добавлены.

### Промпт 6
**Инструмент:** GigaCode AI Agent
**Промпт:** "Создай сервис аутентификации с функциями `register_user` и `authenticate_user`. Проверяй уникальность email, хешируй пароль при регистрации."
**Результат:** Реализован `auth_service.py`. Функция `register_user` проверяет существование email через запрос к БД, хеширует пароль и создаёт пользователя. `authenticate_user` сверяет пароль и возвращает модель при совпадении.

### Промпт 7
**Инструмент:** GigaCode AI Agent
**Промпт:** "Создай маршруты аутентификации: POST /auth/register и POST /auth/login. Используй сервисный слой и возвращай JWT-токен при входе."
**Результат:** Создан `auth.py` в `api/v1`. Маршруты используют `get_db` и `auth_service`. При успешном входе возвращается `access_token` с временем жизни из настроек.

**Итого по этапу 3:**
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Этап 4: CRUD для основных сущностей

### Промпт 8
**Инструмент:** GigaCode AI Agent
**Промпт:** "Сделай полный CRUD для отелей: сервис и маршруты. Добавь фильтрацию, поиск по названию и обработку ошибки 404."
**Результат:** Созданы `hotel_service.py` с функциями `get_hotels`, `get_hotel`, `create`, `update`, `delete`. Созданы маршруты для `/hotels` с валидацией через схемы.

### Промпт 9
**Инструмент:** GigaCode AI Agent
**Промпт:** "Реализуй CRUD для номеров, бронирований и отзывов. Для бронирований — проверка доступности номера и расчёт стоимости. Для отзывов — проверка, что пользователь бронировал отель, и автоматический пересчёт рейтинга."
**Результат:** Созданы `room_service.py`, `booking_service.py`, `review_service.py`. Бронирование проверяет конфликт дат и рассчитывает стоимость. Отзывы обновляют `avg_rating` отеля. Все маршруты защищены.

**Итого по этапу 4:**
- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

---

## Этап 5: Аналитика и админ-панель

### Промпт 10
**Инструмент:** GigaCode AI Agent
**Промпт:** "Создай аналитику для админ-панели: общая статистика, прибыль за 30 дней, загрузка номеров, средний рейтинг, топ-5 отелей. Сделай эндпоинт /admin/analytics только для admin."
**Результат:** Создан `analytics_service.py` с метриками. Эндпоинт защищён зависимостью `get_current_admin`. Статистика собирается агрегатными запросами.

**Итого по этапу 5:**
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 мин

---

## Этап 6: Отладка и исправление ошибок

### Промпт 11
**Инструмент:** GigaCode AI Agent
**Промпт:** "При запуске ошибка: NameError: name 'Boolean' is not defined в моделях. Исправь все импорты в models/room.py, booking.py, review.py, user.py."
**Результат:** Добавлены недостающие импорты `Boolean` и `Text` из sqlalchemy во все файлы моделей. Ошибка устранена.

### Промпт 12
**Инструмент:** GigaCode AI Agent
**Промпт:** "Теперь ошибка: NameError: name 'oauth2_scheme' is not defined в security.py. Исправь порядок объявления."
**Результат:** `oauth2_scheme` перемещён выше по коду, до функции `get_current_user`. Импорт schemas исправлен.

### Промпт 13
**Инструмент:** GigaCode AI Agent
**Промпт:** "Ошибка ImportError в config.py. Функция get_settings не экспортируется. А в security.py неправильный импорт настроек."
**Результат:** В `config.py` добавлена декорированная `@lru_cache` функция `get_settings`. В `security.py` импорт заменён на `from app.core.config import get_settings`.

### Промпт 14
**Инструмент:** GigaCode AI Agent
**Промпт:** "При запуске: 'AsyncEngine' object has no attribute '_run_ddl_visitor'. Я хочу чтобы таблицы создавались автоматически при старте, но движок асинхронный."
**Результат:** Удалён синхронный вызов `create_all`. Рекомендовано использовать Alembic для миграций. Для отладки добавлен совет использовать `run_sync`.

### Промпт 15
**Инструмент:** GigaCode AI Agent
**Промпт:** "Добавь модель Payment в аналитику и исправь review_service — после обновления рейтинга отель не обновляется в сессии."
**Результат:** В модель Payment добавлен импорт `Text`. В `review_service` добавлен вызов `db.refresh(hotel)` после commit. В аналитику добавлены метрики по платежам.

**Итого по этапу 6:**
- Количество промптов: 5
- Что пришлось исправлять вручную: потребовалось вручную удалить строку `Base.metadata.create_all(bind=engine)` из `main.py`, так как агент не мог корректно обработать `run_sync` на асинхронном движке
- Время: ~25 мин

---

## Итоговый отчёт

| Параметр | Значение |
|---|---|
| Всего промптов | 15 |
| Всего этапов | 6 |
| Общее время | ~1 час 35 мин |
| Потребовалось ручных правок | 1 (удаление `create_all`) |
| Создано файлов | ~25 файлов моделей, схем, сервисов, маршрутов |
| Готовность проекта | 100% — все сущности, аутентификация, CRUD, аналитика |
| Использованные инструменты | GigaCode AI Agent |
| Модели данных | User, Hotel, Room, Booking, Review, Payment |
| Основные функции | Регистрация/вход, CRUD отелей/номеров, бронирование, отзывы, аналитика |